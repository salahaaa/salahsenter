# تقرير تشخيص وإصلاح الأعطال

تاريخ الفحص: 2026-06-29

---

## 🩺 ملخّص التشخيص

وجدت **سببين جذرين** يسبّبان كل الأعراض التي ذكرتها. الأول خلل برمجي في الكود (أصلحته)، والثاني ترحيلات قاعدة بيانات مفقودة في الإنتاج (تحتاج تنفيذها).

---

## 🔴 العطل الأول: خطأ "Digest" في صفحة المراجعة والعقد (خلل برمجي) — ✅ تم إصلاحه في الكود

### الأعراض المطابقة لشكواك
- «عند مراجعة طلب فتح المتجر يظهر خطأ مؤقت أثناء تحميل الصفحة»
- «Digest: 1575913912 عند فتح مراجعة العقد»
- «لا يوجد إرسال العقد» (الزر لا يظهر لأن الصفحة تتحطّم قبل رسمه)
- «الدورة ناقصة» (كل عناصر التدفق لا تُرسم بسبب التحطّم)

### السبب الجذري
الملف `components/applications/application-flow-timeline.tsx` موسوم بـ `"use client"`، ويُصدّر دالة `nextApplicationHint()` وكائن `statusLabels`.

لكن **3 صفحات خادم (Server Components)** تستورد وتستدعي هذه الدالة مباشرة:

| الصفحة | كيف تستدعيها |
|--------|--------------|
| `app/admin/merchant-applications/[id]/page.tsx` | `{nextApplicationHint(app.status)}` |
| `app/apply-store/[id]/contract/page.tsx` | `{nextApplicationHint(application.status)}` |
| `app/apply-store/[id]/page.tsx` | `{nextApplicationHint(application.status)}` (مرتين) |

في Next.js App Router **يُمنع استدعاء دوال من وحدات `"use client"` داخل مكونات الخادم**. النتيجة: خطأ وقت التصيير يتحوّل إلى هاش `Digest`.

### الإصلاح المُطبَّق
1. أنشأت ملفاً جديداً آمناً للخادم: **`lib/application-flow.ts`** يحتوي `statusLabels` و `nextApplicationHint` و `ApplicationStatus` (بدون `"use client"`).
2. عدّلت المكوّن التفاعلي ليستورد منهما ويُعيد تصديرهما (للتوافق الخلفي).
3. عدّلت الصفحات الثلاث لتستورد الدوال من `@/lib/application-flow` بدلاً من المكوّن التفاعلي.

### الملفات المُعدَّلة
- ➕ `lib/application-flow.ts` (جديد)
- ✏️ `components/applications/application-flow-timeline.tsx`
- ✏️ `app/admin/merchant-applications/[id]/page.tsx`
- ✏️ `app/apply-store/[id]/contract/page.tsx`
- ✏️ `app/apply-store/[id]/page.tsx`

### التحقق
- ✅ `tsc --noEmit` نظيف
- ✅ صفحة المراجعة تُرجع HTTP 200 وتعرض النص
- ✅ صفحة العقد تُرجع HTTP 200 وتعرض النص
- ✅ لا أخطاء في سجل الخادم

---

## 🟠 العطل الثاني: ترحيلات قاعدة بيانات مفقودة (مشكلة تشغيلية)

### الأعراض المطابقة لشكواك
- «إضافة موظف يظهر خطأ»
- «نظام الصلاحيات لا يوجد أي تغيير فيه»
- أخطاء تسجيل الدخول والصفحة الرئيسية

### السبب الجذري
قاعدة بيانات الإنتاج تفتقد **9 ترحيلات** (من `0010` إلى `0018`). الجداول/الأعمدة المفقودة تشمل:
- `user_mfa_settings` (مع عمود `recovery_code_hashes`) → **يكسر تسجيل الدخول وكل المصادقة**
- `store_offer_collections` → **يكسر الصفحة الرئيسية**
- جداول الموظفين والصلاحيات → **يكسر إضافة الموظفين**

ملفات الترحيل **موجودة في المستودع** (`drizzle/0010_*.sql` إلى `drizzle/0018_*.sql`)، لكنها لم تُنفَّذ على قاعدة بيانات الإنتاج.

### الحل
نفّذ الترحيلات على قاعدة بيانات الإنتاج:

```bash
npm run db:migrate
# أو يدوياً:
npx drizzle-kit migrate
```

بعد تنفيذها اختبرت محلياً: تسجيل الدخول، إنشاء الموظف، حفظ الصلاحيات، ودورة العقد الكاملة — **كلها تعمل**.

---

## ✅ التحقق الكامل للدورة بعد الإصلاح

اختبرت التدفق الكامل لفتح المتجر خطوة بخطوة:

| الخطوة | الانتقال | النتيجة |
|--------|---------|---------|
| تقديم الطلب | → `pending` | ✅ |
| بدء المراجعة | `pending` → `under_review` | ✅ |
| قبول مبدئي | `under_review` → `pre_approved` | ✅ |
| إرسال العقد للتوقيع | `pre_approved` → `contract_created` | ✅ |
| صفحة العقد | تُعرض بدون خطأ | ✅ |
| إضافة موظف + صلاحيات | يُحفظ في قاعدة البيانات | ✅ |

---

## 📋 ما عليك فعله الآن

1. **اسحب الكود المُصلَّح** إلى المستودع (التعديلات أعلاه).
2. **نفّذ الترحيلات** على قاعدة بيانات الإنتاج: `npm run db:migrate`
3. أعد النشر على Vercel.
