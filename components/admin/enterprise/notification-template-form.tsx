"use client";
import { useState, type FormEvent } from "react";
import { useRouter } from "next/navigation";
import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";import { Label } from "@/components/ui/label";import { Textarea } from "@/components/ui/textarea";
export function NotificationTemplateForm(){const router=useRouter();const[message,setMessage]=useState<string|null>(null);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const formElement=e.currentTarget;const f=new FormData(formElement);const variables=String(f.get("variables")||"").split("\n").map(x=>x.trim()).filter(Boolean);const res=await fetch("/api/admin/notification-templates",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({code:f.get("code"),channel:f.get("channel"),titleTemplate:f.get("titleTemplate"),bodyTemplate:f.get("bodyTemplate"),variables,isActive:true})});const data=await res.json();setMessage(res.ok?"✓ تم حفظ القالب":data.message||"تعذر الحفظ");if(res.ok){formElement.reset();router.refresh();}}return <form onSubmit={submit} className="grid gap-4 rounded-3xl border bg-white p-6 shadow-card md:grid-cols-2"><Field label="الكود" name="code" required/><div className="space-y-2"><Label>القناة</Label><select name="channel" className="h-11 w-full rounded-xl border bg-white px-4 text-sm"><option value="in_app">In-App</option><option value="email">Email</option><option value="sms">SMS</option><option value="push">Push</option></select></div><Field label="قالب العنوان" name="titleTemplate" required/><div className="space-y-2"><Label>المتغيرات</Label><Textarea name="variables" placeholder="storeNumber&#10;username"/></div><div className="space-y-2 md:col-span-2"><Label>قالب الرسالة</Label><Textarea name="bodyTemplate" required className="min-h-40"/></div><div className="flex items-center gap-3 md:col-span-2"><Button>حفظ القالب</Button>{message?<span className="text-sm font-bold text-slate-600">{message}</span>:null}</div></form>}
function Field({label,name,required=false}:{label:string;name:string;required?:boolean}){return <div className="space-y-2"><Label>{label}</Label><Input name={name} required={required}/></div>}
