# تقرير فصل نظامي الصلاحيات — Admin vs Merchant Permission Separation

**المهمة:** فصل صلاحيات إدارة المنصة (التي يمنحها الأدمن) عن صلاحيات المتجر (التي يمنحها التاجر)، مع تدفق خطوتين لإدارة الموظفين.
**تاريخ التنفيذ:** 2026-06-29

---

## 🔍 المشكلة المكتشفة

كان المطور السابق قد **خلط** بين نظامي الصلاحيات في صفحة الأدمن:

| المشكلة | الموقع | السبب |
|---------|--------|-------|
| صفحة الأدمن تعرض **كل** الصلاحيات (34) | `app/admin/employees/page.tsx` | `db.select().from(permissions)` بلا فلتر — يُظهر صلاحيات المتجر (`products.manage`, `inventory.manage`...) في مصفوفة موظفي المنصة |
| الأدمن يستطيع منح صلاحية متجر لموظف منصة | لوحة `PlatformEmployeeManagementPanel` | الصلاحيات المرتبطة بالمتجر متاحة في مصفوفة المنصة |
| إضافة الموظف مدمجة مع منح الصلاحية في نموذج واحد | كلتا اللوحتين | لا فصل بين "إنشاء الموظف" و"إدارة صلاحياته" |

**النتيجة:** موظف منصة يمكنه نظرياً الحصول على صلاحية «إدارة منتجات» — وهي صلاحية مرتبطة بمتجر محدد ولا معنى لها لموظف منصة غير مرتبط بمتجر.

---

## ✅ الإصلاحات المنفّذة

### 1) مُصنّف الصلاحيات المركزي `lib/permission-scopes.ts` (جديد)

مصدر واحد للحقيقة يفصل بين نطاقي الصلاحيات:

```
صلاحيات المنصة (Platform)        ← يمنحها الأدمن في صفحة الأدمن
صلاحيات المتجر (Store-bound)     ← يمنحها التاجر في صفحة التاجر
```

- **`STORE_BOUND_PERMISSION_CODES`** — 6 صلاحيات مرتبطة بمتجر (`merchant.access, products.manage, inventory.manage, orders.manage, store_media.manage, store_settings.manage`). تُستبعد تماماً من صفحة الأدمن.
- **`MERCHANT_PERMISSION_CODES`** — مجموعة صلاحيات المتجر الكاملة (تشمل announcements.manage/news.manage لأنها تُفحص scoped للمتجر).
- دوال: `isPlatformPermission()`, `isStorePermission()`.

### 2) تصحيح صفحة الأدمن — صلاحيات المنصة فقط

**قبل:** `db.select().from(permissions)` ← كل الصلاحيات.
**بعد:** `db.select().from(permissions).where(not(inArray(code, STORE_BOUND_CODES)))` ← **28 صلاحية منصة فقط**، صفر صلاحيات متجر.

### 3) إعادة بناء اللوحتين — تدفق الخطوتين

طبّقت نفس النمط الاحترافي على لوحتي الأدمن والتاجر:

**الخطوة 1 — «إضافة موظف بكامل بياناته»**
- نموذج بيانات الموظف الكاملة (الاسم، البريد، الهاتف، الرقم الوظيفي، الهوية، الوظيفة، العنوان، الملاحظات، الحالة، كلمة المرور).
- يُمنح **دخول أساسي تلقائياً** (`admin.access` للمنصة / `merchant.access` للمتجر) — دون فوضى مصفوفة الصلاحيات.

**الخطوة 2 — «إدارة الصلاحيات»**
- من قائمة الموظفين، اضغط «إدارة الصلاحيات» ← يُفتح لوح منفصل **مرتبط بالموظف المُسحوب من القائمة**.
- منح / تعديل / حذف الصلاحيات التفصيلية + اختيار المجموعة الوراثية.
- قابل للحفظ وإعادة التعيين في أي وقت.

هذا يفصل تماماً «إنشاء الشخص» عن «منح الصلاحيات» كما طلبت.

---

## 🧪 التحقق (على خادم حقيقي + قاعدة بيانات)

| الفحص | النتيجة |
|-------|---------|
| `npx tsc --noEmit` | ✅ نظيف |
| `npm run lint` | ✅ نظيف |
| `npm run build` | ✅ نجح كاملاً |
| صفحة الأدمن: صلاحيات المتجر الحاضرة | **0** ✅ |
| صفحة الأدمن: صلاحيات المنصة الحاضرة | 4/4 مفحوصة ✅ |
| صفحة التاجر: صلاحيات المتجر الحاضرة | 6/6 ✅ |
| صفحة التاجر: صلاحيات المنصة الحاضرة | **0** ✅ |
| تدفق الأدمن: إنشاء موظف ← منح صلاحيات | ✅ 201 ثم 200 |
| تدفق التاجر: إنشاء موظف | ✅ 201 |

---

## 📂 الملفات المُعدَّلة

| الملف | التغيير |
|-------|---------|
| `lib/permission-scopes.ts` | **جديد** — مُصنّف النطاق المركزي |
| `app/admin/employees/page.tsx` | فلترة الصلاحيات للمنصة فقط |
| `components/admin/platform-employee-management-panel.tsx` | إعادة بناء كاملة (تدفق خطوتين) |
| `app/merchant/employees/page.tsx` | استخدام المُصنّف المركزي |
| `components/merchant/employee-management-panel.tsx` | إعادة بناء كاملة (تدفق خطوتين) |

---

## ✅ النتيجة

- **صلاحيات التاجر → صفحة التاجر فقط** (إدارة المنتجات/المخزون/الطلبات/إعدادات المتجر).
- **صلاحيات المنصة → صفحة الأدمن فقط** (إدارة المتاجر/الأجنحة/العقود/المستخدمين...).
- لا يمكن للأنظمة أن تختلط بعد الآن — مُصنّف واحد يحكم الفصل.
- تدفق احترافي: أضف الموظف بكامل بياناته ← ثم اسحبه من القائمة لإدارة صلاحياته (إضافة/تعديل/حذف).
