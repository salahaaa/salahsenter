# تقرير فحص نظام المتغيرات وبطاقة الصنف — Variant & Product Card Capability Audit

**النطاق:** تقييم مدى استجابة نظام المتغيرات (variants) وبطاقة الصنف (product card) لاحتياجات **جميع أنواع التجار**، ومرونته مع **المقاسات المختلفة، الألوان المختلفة، والوحدات المختلفة**.
**تاريخ الفحص:** 2026-06-29
**المنهجية:** تحليل معماري عميق لقاعدة البيانات + الـ API + نموذج الإدخال + واجهة عرض الصنف للعميل.

---

## 📊 النتيجة الإجمالية: ✅ المرونة المعمارية ممتازة

النظام مبني على **نمط EAV مُحسّن (Entity-Attribute-Value)** مع جداول علائقية مخصصة — وهو المعيار الذهبي لمنصات التجارة الإلكترونية متعددة التجار. يدعم نظرياً **عدداً غير محدود من أنواع المتغيرات** لكل صنف.

| المعيار | التقييم | الدليل |
|---------|---------|--------|
| مرونة الألوان | 🟢 ممتاز | جدول `colors` + `colorHex` + كشف ذكي بالاسم العربي |
| مرونة المقاسات | 🟢 ممتاز | جدول `sizes` مستقل لكل متجر |
| مرونة الوحدات | 🟢 ممتاز | جدول `units` برمز + استنتاج ذكي للوحدة |
| المتغيرات الديناميكية | 🟢 ممتاز | `productAttributes` + `productAttributeValues` (EAV) |
| توليد التركيبات | 🟢 ممتاز | دالة `cartesian()` تضرب المصفوفات |
| دعم تعدد المتغيرات | 🟢 ممتاز | العبوة + اللون + المقاس معاً |

---

## 🏗️ البنية المعمارية المكتشفة (ممتازة)

النظام يستخدم **طبقتين تكميليتين** للمتغيرات:

### الطبقة 1: متغيرات ثابتة (Legacy / Fast)
جداول مستقلة سريعة: `sizes`, `colors`, `units` — مرتبطة بـ `productVariants` مباشرة (sizeId, colorId, unitId).

### الطبقة 2: متغيرات ديناميكية (Modern / Flexible)
نمط EAV عبر:
- `productAttributes` — تعريف خصائص المتجر (العبوة، المادة، الطراز...) مع `displayType` (button/color/image).
- `productAttributeValues` — قيم كل خاصية (مع `colorHex` و `imageUrl` داعمة).
- `productVariantAttributeValues` — ربط متغير بقيمة (مفتاح مركّب variantId+attributeId).

هذا التصميم يسمح للتاجر بخلق **أي نوع متغير** دون لمس الكود: عبوات، نكهات، خامات، فئات عمرية، إلخ.

---

## ✅ نقاط القوة المُثبتة

### 1) الألوان — معالجة شاملة
- جدول `colors` مع `hexCode` دقيق لكل لون.
- في صفحة العميل: كشف ذكي (`getColorHex`) يطابق **11 لوناً بالعربية والإنجليزية** (أبيض، أسود، وردي، رمادي...) + احترام تطبيع الألف (أ/إ/آ).
- خاصية `colorHex` على مستوى القيمة (`productAttributeValues.colorHex`) تسمح بلون مخصص لأي خاصية ديناميكية.
- واجهة العرض: دوائر ألوان مرئية (`backgroundColor`) + نص.

### 2) الوحدات — ذكاء في الاستنتاج
- جدول `units` برمز (`symbol`) — يدعم "حبة، كيس، كرتون (20 حبة)، درزن (12 حبة)".
- **استنتاج ذكي** (`inferUnitId`): عند توليد التركيبات، النظام يطابق قيمة المتغير (مثل "كرتون") مع اسم الوحدة تلقائياً — يوفر على التاجر اختيار الوحدة يدوياً لكل تركيبة.
- يطابق بالاسم والرمز والجمع مع تطبيع نصي.

### 3) المقاسات — مستقلة ومرنة
- جدول `sizes` لكل متجر (الاسم يُعرّفه التاجر: S/M/L، 38/40/42، مقاسات ملابس أطفال...).
- غير مقيّد بقيم محددة سلفاً — التاجر يُعرّف نظام مقاساته الخاص.

### 4) توليد التركيبات (Cartesian Product)
- دالة `cartesian()` تضرب قيم المتغيرات: العبوة(3) × اللون(2) × المقاس(3) = **18 تركيبة** بضغطة زر.
- كل تركيبة لها: SKU مستقل، باركود، سعر مستقل، مخزون مستقل، صورة مستقلة.
- التاجر يراجع الجدول قبل الحفظ ويعدّل أي تركيبة.

### 5) بطاقة الصنف للعميل — تجربة سلسة
- اختيار المتغيرات تفاعلي (`selectOption`): عند اختيار "كرتون + أحمر" يبحث عن التركيبة المطابقة.
- المعرض يتكيف: يعرض صور التركيبة المختارة أولاً.
- السلة المحلية تحفظ `attributes` الكاملة للتركيبة.
- الكمية محدودة بالمخزون الفعلي للتركيبة المختارة.

---

## 🟡 الفجوات المكتشفة (تحتاج تطويراً)

### الفجوة 1: 🔴 اختيار متعدد المتغيرات بدون تعطيل التركيبات غير المتاحة (UX)

**المشكلة:** عند منتج بـ (العبوة: كيس/كرتون) + (اللون: أحمر/أزرق) = 4 تركيبات. لو التركيبة "كرتون + أزرق" نفد مخزونها أو غير موجودة، الواجهة **لا تُعطّل** زر "أزرق" عندما يكون "كرتون" مختاراً. المستخدم قد يختار تركيبة غير موجودة فيقع على fallback.

**الخطورة:** 🟡 متوسطة — تجربة مستخدم مربكة في المنتجات متعددة المتغيرات.
**الموقع:** `components/product/product-detail.tsx` — دالة `selectOption` وعرض الأزرار.

### الفجوة 2: 🟡 عنوان التركيبة في حالة المتغير الديناميكي الواحد
**المشكلة:** `buildVariantTitle` يجمع قيم السمات بـ "/". لكن عند متغير واحد فقط (مثل العبوة = كيس) يظهر العنوان كـ "العبوة: كيس" بدل "كيس" فقط. شكلي لكن يؤثر على بطاقة السلة.
**الخطورة:** 🟢 منخفضة.

### الفجوة 3: 🟡 غياب "صورة لكل قيمة متغير" في المعرض الديناميكي
**المشكلة:** `productAttributeValues.imageUrl` موجود في الـ schema (يدعم صورة لكل قيمة، مثل صورة لكل لون)، لكن واجهة العرض لا تستفيد منها لإظهار صورة القيمة عند التحويم عليها (hover/selection).
**الخطورة:** 🟡 متوسطة — ميزة متوقعة في متاجر الأزياء (hover على لون يُظهر الصورة المعنية).

### الفجوة 4: 🟢 لا توجد حدود حماية لتعقيد التركيبات
**المشكلة:** لا حد أقصى لعدد التركيبات المولّدة. تاجر يختار 5 متغيرات × 5 قيم لكل = 3125 تركيبة. قد يسبب تجميد المتصفح وحفظ بطيء.
**الخطورة:** 🟢 منخفضة (نادر) لكن يستحق حداً أمانياً.

---

## ✅ التحقق من تغطية أنواع التجار

| نوع التاجر / الصنف | مدعوم؟ | الآلية |
|---------------------|--------|--------|
| **تجزئة الأزياء** (لون + مقاس) | ✅ نعم | colors + sizes + توليد تركيبات |
| **بقالة / سوبر ماركت** (وحدات/عبوات) | ✅ نعم | units + عبوة كخاصية ديناميكية |
| **إلكترونيات** (سعة/ذاكرة/لون) | ✅ نعم | خصائص ديناميكية + قيم |
| **مواد بناء** (مقاسات غير قياسية) | ✅ نعم | sizes مفتوحة + خصائص |
| **مطاعم** (حجم/إضافات) | ✅ نعم | خصائص ديناميكية |
| **مجوهرات/ساعات** (معدن/قيراط) | ✅ نعم | خصائص ديناميكية |
| **منتج واحد بسيط** (بدون متغيرات) | ✅ نعم | variant افتراضي تلقائي |

**الخلاصة:** البنية المعمارية **تغطي كل أنواع التجار** بفضل نمط EAV.

---

## 📋 التوصيات (مرتبة بالأولوية)

### 1) 🔴 تطوير تعطيل التركيبات غير المتاحة (UX حرج)
في `product-detail.tsx`، احسب القيم المتاحة لكل خاصية بناءً على الاختيار الحالي، وعرّض الأزرار غير المتاحة كـ **رمادية/معطّلة** بدل تركها قابلة للنقر. هذا سلوك متوقع في أي منصة احترافية (مثل Amazon/Noon).

### 2) 🟡 إظهار صورة قيمة المتغير عند الاختيار
استغل `productAttributeValues.imageUrl` لتبديل الصورة المعروضة عند اختيار لون/طراز — يرفع معدل التحويل في الأزياء.

### 3) 🟢 حد أقصى للتركيبات (حماية)
أضف تحذيراً عند تجاوز عدد التركيبات (مثلاً 200) مع تأكيد قبل التوليد.

---

## 📂 الملفات المرتبطة (للمرجع)

| الملف | الدور |
|-------|-------|
| `lib/db/schema.ts` | تعريف الجداول (sizes/colors/units/attributes/values/variants) |
| `components/merchant/product-engine-form.tsx` | نموذج الإدخال + توليد التركيبات |
| `components/product/product-detail.tsx` | عرض الصنف للعميل + اختيار المتغيرات |
| `app/store/[slug]/products/[productSlug]/page.tsx` | صفحة الصنف (تحميل المتغيرات + inline media) |
| `lib/validators.ts` | `productSchema` + `productVariantInputSchema` |
| `lib/product-coding.ts` | توليد الأكواد + `buildVariantTitle` |

---

## ✅ الخلاصة

نظام المتغيرات وبطاقة الصنف **مرن ومكتمل معماريًا** ويخدم جميع أنواع التجار والمقاسات والألوان والوحدات عبر نمط EAV احترافي. النقاط الوحيدة التي تستحق التطوير هي **تحسينات تجربة المستخدم** في واجهة العميل (تعطيل التركيبات غير المتاحة + صور القيم عند الاختيار) — وليست قيوداً معمارية.
