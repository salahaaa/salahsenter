export const dynamic="force-dynamic";import { and,eq } from "drizzle-orm";import { z } from "zod";import { fail,handleApiError,ok } from "@/lib/api";import { requireAuth } from "@/lib/auth";import { db,mediaAssets } from "@/lib/db";import { analyzeAiImage } from "@/lib/ai/vision";const schema=z.object({assetUrl:z.string().min(1)});export async function POST(request:Request){try{const session=await requireAuth();const payload=schema.parse(await request.json());const [asset]=await db.select().from(mediaAssets).where(and(eq(mediaAssets.ownerId,session.userId),eq(mediaAssets.url,payload.assetUrl))).limit(1);if(!asset||!asset.mimeType?.startsWith("image/"))return fail("اختر صورة مرفوعة تملكها لتحليل المواصفات",422);return ok(await analyzeAiImage({imageUrl:asset.url,purpose:"product_specs"}));}catch(error){return handleApiError(error,"تعذر تحليل مواصفات الصورة");}}
