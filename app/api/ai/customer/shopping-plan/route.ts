export const dynamic="force-dynamic";import { z } from "zod";import { fail,handleApiError,ok } from "@/lib/api";import { checkIpRateLimit } from "@/lib/rate-limit";import { planCustomerShopping } from "@/lib/ai/specialists";const schema=z.object({query:z.string().trim().min(2).max(160)});export async function POST(request:Request){try{const rate=await checkIpRateLimit("ai:customer-shopping",30,10*60*1000);if(!rate.allowed)return fail("محاولات كثيرة، حاول لاحقاً",429);const payload=schema.parse(await request.json());return ok(await planCustomerShopping(payload.query));}catch(error){return handleApiError(error,"تعذر تخطيط التسوق بالذكاء");}}
