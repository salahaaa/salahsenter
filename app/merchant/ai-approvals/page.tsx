import Link from "next/link";import { CheckCircle2 } from "lucide-react";import { SiteHeader } from "@/components/layout/site-header";import { Button } from "@/components/ui/button";import { requireAuth } from "@/lib/auth";import { listAiProposals } from "@/lib/ai/workbench";import { AiApprovalInbox } from "@/components/ai/ai-approval-inbox";export default async function MerchantAiApprovals(){const session=await requireAuth();const items=await listAiProposals({userId:session.userId,audience:"merchant"});return <main className="min-h-screen merchant-aurora"><SiteHeader/><section className="container py-8"><div className="mb-8 flex justify-between"><div><CheckCircle2 className="h-8 w-8 text-violet-600"/><h1 className="mt-3 text-3xl font-black">موافقات الذكاء</h1></div><Button asChild variant="outline"><Link href="/merchant/ai-workbench">مركز الذكاء</Link></Button></div><AiApprovalInbox initial={JSON.parse(JSON.stringify(items))}/></section></main>}
