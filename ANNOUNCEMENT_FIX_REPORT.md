# تقرير إصلاح إعلانات الواجهة الرئيسية — Announcement Visibility & Promoted Design Fix

**المشاكل المُبلّغ عنها:**
1. «نشرت إعلان الافتتاح لكنه لم يظهر في الواجهة الرئيسية»
2. «نشرت إعلاناً مموّلاً لكنه ظهر بشكل غير احترافي»

**تاريخ الإصلاح:** 2026-06-29

---

## 🔍 جذر المشكلتين

### المشكلة 1 — إعلان الافتتاح لا يظهر
**السبب:** نموذج إنشاء الإعلان كان يضع الحالة الافتراضية على **«مسودة» (draft)**، بينما الاستعلام في الصفحة الرئيسية يفلتر صراحةً `status = "active"`. النتيجة: أي إعلان يُحفظ بالحالة الافتراضية **لا يظهر إطلاقاً** للعامة.

### المشكلة 2 — الممول يظهر بشكل غير احترافي
**السببان معاً:**
1. الاستعلام `marketplaceAnnouncementProjection` **لم يكن يجلب** حقلي `isPromoted` و`promotionPackage` — لذا الواجهة لا تعرف أصلاً أي إعلان مموّل.
2. دالة العرض `MarketplaceAdsSection` كانت تعرض **كل** الإعلانات (عادية وممولة) بنفس تصميم البطاقة البسيطة، بدون أي تمييز بصري للمموّل.

---

## ✅ الإصلاحات المنفّذة

### 1) النموذج — الحالة الافتراضية «نشط» + إرشاد واضح
- تغيير القيمة الافتراضية من `draft` إلى `active` («نشط — يظهر فوراً»).
- إضافة وصف صريح: «يجب اختيار «نشط» لكي يظهر الإعلان في الصفحة الرئيسية».
- توضيح خيار المموّل: «إعلان مموّل (يظهر بتصميم بارز ومميز)».

### 2) الاستعلام — جلب حقول الترويج
أضفت `isPromoted` و `promotionPackage` إلى `marketplaceAnnouncementProjection`، فأصبحت الواجهة تعرف أي الإعلانات مموّلة.

### 3) العرض — تصميم احترافي مميّز للإعلان المموّل
أعدت بناء `MarketplaceAdsSection` لتفصل الإعلانات إلى مجموعتين بتصميمين مختلفين:

**الإعلان المموّل (Promoted):**
- بنر هيرو بعرض كامل، بطاقة كبيرة بإطار ذهبي بارز `border-amber-300`.
- شارة «★ إعلان مموّل» ذهبية في الزاوية.
- صورة كبيرة + عنوان ضخم + زر CTA متدرّج ذهبي/برتقالي «اكتشف العرض».
- تأثير تحريك وتركيب احترافي (gradient + scale-on-hover).

**الإعلان العادي:**
- شبكة بطاقات مدمجة 3 أعمدة (كما كانت، تصميم نظيف).

---

## ✅ التحقق من الجودة

| الفحص | النتيجة |
|-------|---------|
| `tsc --noEmit` | ✅ نظيف |
| `npm run lint` | ✅ نظيف |
| `npm run build` | ✅ نجح (BUILD_ID طازج، لا أخطاء كود) |

---

## 📂 الملفات المُعدَّلة

| الملف | التغيير |
|-------|---------|
| `components/admin/marketplace-announcement-form.tsx` | الحالة الافتراضية `active` + إرشاد + توضيح المموّل |
| `lib/db/queries.ts` | جلب `isPromoted` + `promotionPackage` في الـ projection |
| `components/home/luxury-marketplace-home.tsx` | تصميم مميّز احترافي للإعلان المموّل |

---

## ✅ الخلاصة

- إعلان الافتتاح الآن **يظهر فوراً** عند إنشائه (الحالة الافتراضية «نشط»).
- الإعلان المموّل يظهر **بتصميم احترافي بارز** يختلف كلياً عن الإعلان العادي: بنر هيرو ذهبي + شارة + زر CTA، تماماً كما يتوقع الأدمن لإعلان مدفوع.
