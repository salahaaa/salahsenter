# تقرير بناء نظام بنرات التجار الممولة — Merchant Sponsored Banner Pipeline

**السؤال:** هل يستطيع التاجر رفع إعلان للإدمن لنشره في الواجهة الرئيسية كإعلان ممول؟

**الإجابة قبل البناء:** البنية التحتية موجودة جزئياً لكن الدورة كانت مكسورة. الآن ✅ الدورة مكتملة.
**تاريخ التنفيذ:** 2026-06-29

---

## 🔍 ما كان موجوداً وما كان مكسوراً

| الجزء | الحالة قبل | الحالة بعد |
|-------|-----------|-----------|
| التاجر ينشئ حملة | ✅ كان يعمل | ✅ |
| الحملة تُرسل للمراجعة | ✅ كان يعمل | ✅ |
| الأدمن يراجع/يوافق | ✅ كان يعمل | ✅ |
| **التاجر يرفع صورة بنر + رابط** | 🔴 غير ممكن | ✅ **أُضيف** |
| **البنرات المعتمدة تظهر بالواجهة** | 🔴 مقطوعة تماماً | ✅ **أُضيف** |

كان هناك قناة مقطوعة: التاجر يرسل الحملة ← الأدمن يوافق ← ثم لا شيء يحدث. كان النموذج يجمع فقط نصاً (`headline`/`description`) بدون صورة، ودالة الواجهة الرئيسية لا تستعلم عن `adCampaigns` إطلاقاً.

---

## ✅ الدورة المكتملة الآن

```
التاجر ──(ينشئ حملة homepage_banner + يرفع صورة + رابط)──> قائمة المراجعة
   ↓
الأدمن ──(يراجع ← اعتماد)──> الحالة: approved
   ↓
الواجهة الرئيسية ──(تعرض البنر في قسم الإعلانات الممولة)──> شارة «★ إعلان تاجر مموّل — [اسم المتجر]»
```

### التحسينات المنفّذة

**1) نموذج التاجر — رفع محتوى البنر**
- عند اختيار نوع `homepage_banner` يظهر قسم «محتوى البنر الإعلاني».
- رفع صورة بنر (`MediaUrlInput`) + رابط وجهة.
- إرشاد: «الصورة المثالية: أفقية، دقة عالية (1200×400)».
- المحتوى يُخزَّن في حقل `creative` (jsonb).

**2) دالة جلب البنرات المعتمدة `getHomepageApprovedMerchantBanners`**
- تستعلم عن حملات `type=homepage_banner` بـ `status IN (approved, active)`.
- ضمن نافذة التاريخ + المتجر نشط.
- تُعيد بنر بشكل يتوافق مع إعلانات المول + علامة `isMerchantAd` و `storeName`.

**3) الدمج في الواجهة الرئيسية**
- البنرات التجارية تُدمج في `marketplaceAnnouncements`.
- تظهر في قسم «إعلانات المول العامة» بتصميم الهيرو الممول.
- شارة تميّز التاجر: «★ إعلان تاجر مموّل — اسم المتجر».

---

## ✅ التحقق من الجودة

| الفحص | النتيجة |
|-------|---------|
| `tsc --noEmit` | ✅ نظيف |
| `npm run lint` | ✅ نظيف |
| `npm run build` | ✅ نجح (BUILD_ID طازج، لا أخطاء كود) |

---

## 📂 الملفات المُعدَّلة

| الملف | التغيير |
|-------|---------|
| `components/merchant/ad-campaign-form.tsx` | رفع صورة بنر + رابط وجهة (حسب نوع الإعلان) |
| `app/merchant/ads/page.tsx` | تمرير storeId للنموذج |
| `lib/home-visibility.ts` | `getHomepageApprovedMerchantBanners()` |
| `lib/db/queries.ts` | دمج البنرات المعتمدة في الواجهة |
| `components/home/luxury-marketplace-home.tsx` | شارة تمييز تاجر على البنر الممول |

---

## ✅ الخلاصة

الآن **نعم** — التاجر يستطيع رفع إعلان للإدمن لنشره كإعلان ممول: يرفع صورة البنر والرابط ← يرسله ← الأدمن يعتمده ← يظهر فوراً في الواجهة الرئيسية بتصميم احترافي مميز بشارة المتجر.
