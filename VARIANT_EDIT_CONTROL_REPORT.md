# تقرير إصلاح تحكّم التاجر الكامل بمتغيراته — Full Variant Control Fix

**المشكلة التي أبلغت عنها:** «نشرت صنفاً ونسيت إضافة اللون، وعندما عدت لأعدّله وأضيف اللون لم أستطع. تأكد أن التاجر لديه التحكم الكامل بمنتجاته ومتغيراته وليس هناك شيء مفروض عليه.»

**تاريخ الإصلاح:** 2026-06-29

---

## 🔍 جذر المشكلة المكتشف

كان نموذج تعديل المنتج **يعرض المتغيرات كصفوف ثابتة** (عنوان/SKU/سعر فقط) **بدون أي محرر سمات**. تحديداً:

| العطل | الموقع |
|-------|--------|
| `editVariantSchema` لم يستقبل `attributeValueIds` ولا `unitId/sizeId/colorId` | `app/api/merchant/products/[id]/route.ts` |
| الـ API **لم يحدّث** جدول `productVariantAttributeValues` إطلاقاً عند التعديل | نفس الملف — كان يحدّث فقط SKU/السعر/المخزون |
| النموذج لم يعرض الخصائص (اللون/المقاس/العبوة) كأزرار قابلة للتغيير | `components/merchant/product-edit-form.tsx` |

**النتيجة:** التاجر يستطيع تغيير السعر والمخزون فقط — لكن **لا يمكنه إضافة لون، أو تغيير اللون، أو حذفه** من متغيّر موجود. هذا بالضبط ما واجهته.

---

## ✅ الإصلاحات المنفّذة (3 طبقات)

### 1) الـ API — مزامنة كاملة لجدول السمات
- `editVariantSchema` الآن يستقبل: `attributeValueIds`, `unitId`, `sizeId`, `colorId`.
- داخل المعاملة، عند تحديث/إنشاء أي متغيّر:
  - يُحذف الروابط القديمة في `productVariantAttributeValues`.
  - تُربط القيم الجديدة المختارة (مع جلب `attributeId` المطابق من `productAttributeValues`).
  - خصم مكرر `attributeId` للحفاظ على صحة المفتاح الأساسي المركّب `(variantId, attributeId)`.
- `GET` الآن يرجع `attributeValueIds` لكل متغيّر (لإعادة الربط في النموذج).

### 2) الصفحة — تمرير كل بيانات التصنيف
صفحة التعديل الآن تجلب وتمرّر للنموذج:
- `attributes` + `attributeValues` (الخصائص وقيمها)
- `units`, `sizes`, `colors`
- `attributeValueIds` الحالية لكل متغيّر

### 3) النموذج — محرر سمات تفاعلي لكل متغيّر
أعدت بناء النموذج بالكامل. الآن لكل متغيّر:
- **محرر خصائص** بأزرار تبديل: اضغط لون/مقاس/عبوة لإضافته أو إزالته.
- عند التغيير، يُعاد بناء `attributes` والعنوان تلقائياً.
- **وحدة بيع** قابلة للتغيير لكل متغيّر.
- «إضافة متغير» يبدأ بصفحة بيضاء يختار فيها التاجر أي خصائص يشاء.
- تعطيل بدل الحذف (يحفظ الطلبات القديمة).

---

## ✅ تأكيد: التاجر يتحكم بالكامل، لا شيء مفروض

تحققت من أنه **لا يوجد قيد يفرض شيئاً** على التاجر:

| التحكم | مملوك للتاجر؟ |
|--------|---------------|
| إضافة لون لمتغيّر موجود | ✅ الآن نعم |
| تغيير اللون من أحمر إلى أزرق | ✅ الآن نعم |
| إزالة لون من متغيّر | ✅ الآن نعم |
| تغيير المقاس/العبوة/الوحدة | ✅ الآن نعم |
| إضافة متغيّر جديد بخصائصه | ✅ |
| حذف (تعطيل) متغيّر | ✅ |
| تغيير السعر/المخزون/SKU/الصورة | ✅ |
| عدد المتغيرات | ✅ غير محدد (القيد الوحيد: سعر>0 عند النشر) |

القيد الوحيد المتبقي منطقي وغير مفروض تعسفياً: **لا يمكن نشر منتج نشط بدون سعر أكبر من صفر**.

---

## 🧪 التحقق من الجودة

| الفحص | النتيجة |
|-------|---------|
| `tsc --noEmit` | ✅ نظيف |
| `npm run lint` | ✅ نظيف |
| `npm run build` | ✅ نجح (BUILD_ID طازج، لا أخطاء) |

---

## 📂 الملفات المُعدَّلة

| الملف | التغيير |
|-------|---------|
| `app/api/merchant/products/[id]/route.ts` | استقبال السمات + مزامنة `productVariantAttributeValues` + GET يرجع الروابط |
| `app/merchant/products/[id]/edit/page.tsx` | تمرير الخصائص/القيم/الوحدات/المقاسات/الألوان |
| `components/merchant/product-edit-form.tsx` | محرر سمات تفاعلي + وحدة بيع لكل متغيّر + إعادة بناء كاملة |

---

## ✅ الخلاصة

أصبح التاجر يملك **التحكم الكامل** بكل متغيّرات منتجاته: يمكنه إضافة/تغيير/حذف الألوان والمقاسات والعبوات والوحدات على أي منتج — حتى لو نُسي عند الإنشاء. لا شيء مفروض عليه سوى القاعدة المنطقية الواحدة (سعر>0 عند النشر).
