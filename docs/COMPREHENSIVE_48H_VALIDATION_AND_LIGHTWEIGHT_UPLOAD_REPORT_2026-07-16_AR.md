# تقرير الفحص الشامل وتخفيف حزمة الرفع — آخر 48 ساعة

**التاريخ:** 2026-07-16  
**النطاق:** التغييرات المحلية خلال آخر 48 ساعة، وبالأخص: قوالب الأنشطة وربط الجناح، تجربة التاجر، العروض كمنتجات مخزنية، الظهور في الرئيسية، وحزمة تطوير الإعلانات الجارية.

## الفحص الآلي المنفذ

| الفحص | النتيجة |
|---|---|
| ESLint | ناجح |
| TypeScript `tsc --noEmit` | ناجح |
| Vitest | ناجح: 61 ملفاً / 167 اختباراً |
| journal migrations | ناجح: 84 SQL / 84 journal entries |
| Drizzle check | ناجح |
| فحص الأسرار | ناجح: لا مفاتيح حية أو `.env` runtime متتبعة |
| حراس مسارات الأدمن | ناجح |
| `npm audit --omit=dev --audit-level=high` | لا ثغرات عالية |
| `git diff --check` | ناجح |
| أسماء الملفات والمسارات | ناجح: ASCII-safe للرفع |

## أخطاء تم العثور عليها وإصلاحها أثناء الفحص

1. عدم اتساق اختبارين بعد تحويل يوم الإعلانات التشغيلي إلى `Asia/Aden` وتغيير dedupe الظهور إلى bucket قصير لدعم frequency cap. تم تحديث الاختبارات لتطابق السياسة الجديدة، ثم نجحت المجموعة كاملة.
2. خطأ TypeScript في نافذة سبب رفض إعلان وفي timer الخاص بتتبع الظهور. تم إصلاحه، ثم نجح lint وtypecheck.

## فحص جودة إضافي

### الوصولية static audit

- كل الصور المفحوصة تملك `alt`.
- الفحص أحصى 50 input بلا identity صريح و57 native dialogs في المشروع ككل.
- هذه ملاحظات تراكمية للمشروع وليست فشل build؛ تشمل ملفات قديمة وملفات تطوير حديثة. تحتاج خطة a11y مستقلة لاستبدال native dialogs وإكمال labels/aria للمدخلات.

### فحوص لم يمكن تأكيدها محلياً

| الفحص | السبب |
|---|---|
| Bundle budget | لا توجد `.next/static/chunks` لأن `next build` لم يشغل؛ بيئة Arena محدودة الذاكرة. |
| Media inline audit | يحتاج `DATABASE_URL` حقيقية، ولم يستخدم اتصال قاعدة بيانات حية. |
| PostgreSQL integration | لا توجد قاعدة Staging متصلة. |
| Playwright/Lighthouse/Axe على واجهة حية | يحتاج تطبيقاً منشوراً أو Staging. |
| Redis/Cron/Sentry/ERP | لم تستخدم خدمات حية. |

## تخفيف حجم المشروع للرفع

- الحجم أثناء وجود dependencies المحلية: نحو **932 MB**.
- الحجم المصدرّي بعد حذف dependencies والكاش: نحو **24 MB**.
- أُنشئت حزمة رفع مصدرية كاملة خفيفة تستبعد:

```text
node_modules
.git
.next
coverage
.cache
build
dist
out
.review
runtime .env files
```

وتحتفظ بالكود، migrations، journal، الاختبارات، الوثائق، `package-lock.json`، والخط العربي المطلوب للعقود PDF.

- حجم الحزمة المضغوطة الناتجة: **3.2 MB**.
- تم اختبار سلامة ملف ZIP بعد إنشائه بنجاح.

بعد الرفع أو التنزيل يكفي:

```bash
npm ci
```

ولا تتضمن الحزمة أسراراً أو dependencies مولدة.

## الحكم

الفحص المحلي الآلي ناجح، لكن لا يعني اعتماد Staging أو Production. قبل النشر يجب تنفيذ migrations على Staging بصورة معتمدة ثم اختبار دورات العروض والإعلانات والواجهات على قاعدة وخدمات حقيقية.
